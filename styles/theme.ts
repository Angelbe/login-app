import { ETheme } from "src/interfaces/theme.interface";

const {
  defaultFontSize,
  mainColor,
  mainColorlighter,
  secundaryColorLight,
  secundaryColorLighter,
  secundaryColor,
  disabledColor,
  borderRadius,
} = ETheme;

const theme = {
  defaultFontSize,
  mainColor,
  mainColorlighter,
  secundaryColor,
  secundaryColorLighter,
  secundaryColorLight,
  disabledColor,
  borderRadius,
};

export { theme };
